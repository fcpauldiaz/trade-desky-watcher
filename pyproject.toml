[project]
name = "trade-desky-watcher"
version = "1.4.6"
description = "Trade Desky Watcher — capture OS notifications and forward them to Trade Desky"
readme = "README.md"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
